/*
 * Used to route URLs and to build a sitemap, this file is used as data
 * for X-macros.
 */

HTML("about-json-api", about_json_api)
HTML("status", status)
HTML("about", about)
JSON("about", about)
TXT("robots", robots)
XML("sitemap", sitemap)

DIR("search")
	HTML("players", search)
	HTML("clans",   search)
	HTML("servers", search)
END()
HTML("search", search)

DIR("player")
	DIR(NULL)
		SVG("historic", graph)
	END()
	JSON(NULL, player)
	HTML(NULL, player)
END()

DIR("clan")
	JSON(NULL, clan)
	HTML(NULL, clan)
END()

DIR("server")
	JSON(NULL, server)
	HTML(NULL, server)
END()

DIR("pages")
	HTML(NULL, teerank2_player_list)
END()

#define LIST(name)                                                      \
DIR(name "s")                                                           \
	DIR(NULL)                                                       \
		JSON(NULL, list)                                        \
		HTML(NULL, list)                                        \
	END()                                                           \
	JSON(NULL, list)                                                \
	HTML(NULL, list)                                                \
END()                                                                   \
HTML(name "s", list)                                                    \
JSON(name "s", list)

LIST("player")
LIST("clan")
LIST("server")

DIR("players")
	DIR(NULL)
		SVG("elo rank", teerank3_graph)
	END()
END()

/* Root */
HTML("", list)

#undef DIR
#undef END
#undef HTML
#undef JSON
#undef TXT
#undef XML
#undef SVG
