# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build scheduler`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t scheduler`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system scheduler && \
          adduser --system -G scheduler scheduler

COPY dist/apps/scheduler .
RUN chown -R scheduler:scheduler .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --omit=dev -f install

COPY libs/prisma/prisma/schema.prisma prisma/schema.prisma
RUN npx prisma generate

CMD [ "node", "." ]
